# Hydra `_target` Autocomplete


Adds completion for YAML values under `_target:` in Hydra configs.


## Run locally


```bash
# Choose your IDE in gradle.properties (platformType=PY|PC) and version
./gradlew runIde